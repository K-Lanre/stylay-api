# Server Configuration
NODE_ENV=development
PORT=3000

# Application Name
APP_NAME=Stylay

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=stylay_db
DB_USER=root
DB_PASSWORD=
DB_DIALECT=mysql

# JWT Configuration
JWT_SECRET=5a8f3d9c1e7b2f6d4a0c9e8b7f2d5a1c3e6f9b8d7a4c2e1f0b5d8a3c6e9f7b2
JWT_EXPIRES_IN=90d
JWT_COOKIE_EXPIRES_IN=90

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60 * 60 * 1000 # 1 hour
RATE_LIMIT_MAX=1000

# Security
CORS_ORIGIN=http://localhost:3001

# Logging
LOG_LEVEL=info

# Email Configuration (for future use)
EMAIL_HOST=smtp.ethereal.email
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=eunice.kulas72@ethereal.email
EMAIL_PASS=qz1VDFMbCebZswXdMw
EMAIL_FROM=noreply@stylay.com
EMAIL_FROM_NAME=Stylay

# AWS S3 Configuration (for file uploads)
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_REGION=your_aws_region
# AWS_BUCKET_NAME=your_bucket_name

# Frontend URL (for CORS and email templates)
FRONTEND_URL=http://localhost:3000

# Session Secret (for express-session)
SESSION_SECRET=your_session_secret_here

# Password Reset Token Expiration (in minutes)
PASSWORD_RESET_EXPIRES=30

# Default User Type Password
DEFAULT_VENDOR_PASSWORD=Vendor@123
DEFAULT_CUSTOMER_PASSWORD=Secret123!
DEFAULT_ADMIN_PASSWORD=Admin@123


# Environment-specific overrides (these will override the above settings)
# DB_HOST=production-db-host
# ... other production-specific settings
