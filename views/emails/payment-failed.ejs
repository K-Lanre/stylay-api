<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= subject %></title>
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { text-align: center; padding: 20px 0; border-bottom: 1px solid #eee; }
    .logo { max-width: 150px; }
    .alert { 
      background-color: #fff3f3; 
      border-left: 4px solid #f44336;
      padding: 15px;
      margin: 20px 0;
      border-radius: 4px;
    }
    .btn {
      display: inline-block; padding: 10px 20px; background: #f44336; color: white;
      text-decoration: none; border-radius: 4px; margin: 10px 0;
    }
    .btn.secondary {
      background: #666; margin-left: 10px;
    }
    .footer { margin-top: 30px; text-align: center; font-size: 12px; color: #777; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <img src="<%= process.env.LOGO_URL %>" alt="Stylay" class="logo">
      <h1>Payment Failed</h1>
      <p>We couldn't process your payment</p>
    </div>

    <div class="alert">
      <h2>Payment Unsuccessful</h2>
      <p>We were unable to process your payment for Order #<%= order.id %>. Your order has been placed on hold.</p>
      <p><strong>Reason:</strong> <%= order.payment_error || 'Payment was declined' %></p>
    </div>

    <div>
      <p>To complete your purchase, please try again with a different payment method:</p>
      <div style="margin: 20px 0;">
        <a href="<%= retryUrl %>" class="btn">Retry Payment</a>
        <a href="<%= process.env.FRONTEND_URL %>/contact" class="btn secondary">Contact Support</a>
      </div>
      
      <p>If you believe this is an error, please contact your bank or card issuer for more information.</p>
    </div>

    <div style="margin: 30px 0; padding: 15px; background: #f9f9f9; border-radius: 5px;">
      <h3>Order Summary</h3>
      <p><strong>Order #<%= order.id %></strong></p>
      <p><strong>Order Total:</strong> $<%= (order.total_amount || 0) %></p>
      <p><strong>Items:</strong> <%= order.items.reduce((sum, item) => sum + item.quantity, 0) %></p>
    </div>

    <div class="footer">
      <p>Need help? Contact our support team at <%= process.env.SUPPORT_EMAIL || 'support@stylay.com' %></p>
      <p>© <%= new Date().getFullYear() %> Stylay. All rights reserved.</p>
    </div>
  </div>
</body>
</html>
