<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= subject %></title>
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { text-align: center; padding: 20px 0; border-bottom: 1px solid #eee; }
    .logo { max-width: 150px; }
    .cancellation-notice { 
      background-color: #ffebee; 
      border-left: 4px solid #f44336;
      padding: 20px;
      margin: 20px 0;
      border-radius: 4px;
    }
    .order-details { 
      background: #f9f9f9; 
      padding: 15px; 
      border-radius: 5px; 
      margin: 20px 0;
    }
    .btn {
      display: inline-block; 
      padding: 10px 20px; 
      background: #f44336; 
      color: white;
      text-decoration: none; 
      border-radius: 4px; 
      margin: 10px 5px;
    }
    .btn.secondary {
      background: #666;
    }
    .footer { 
      margin-top: 30px; 
      text-align: center; 
      font-size: 12px; 
      color: #777; 
      border-top: 1px solid #eee;
      padding-top: 15px;
    }
    .order-item {
      display: flex;
      margin: 10px 0;
      padding: 10px 0;
      border-bottom: 1px solid #eee;
    }
    .item-image {
      width: 60px;
      height: 60px;
      background: #f0f0f0;
      margin-right: 15px;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
    }
    .item-details {
      flex-grow: 1;
    }
    .item-name {
      font-weight: bold;
      margin: 0 0 5px 0;
    }
    .item-price {
      color: #e53935;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <img src="<%= logoBase64 %>" alt="Stylay" class="logo">
      <h1>Order #<%= order.id %> Cancelled</h1>
    </div>

    <div class="cancellation-notice">
      <h2>Your Order Has Been Cancelled</h2>
      <p>We're sorry to inform you that your order #<%= order.id %> has been cancelled.</p>
      
      <% if (reason) { %>
        <p><strong>Reason for cancellation:</strong> <%= reason %></p>
      <% } %>
      
      <% if (order.payment_status === 'refunded') { %>
        <p>Your payment has been refunded. Please allow 3-5 business days for the refund to appear in your account.</p>
      <% } else if (order.payment_status === 'pending_refund') { %>
        <p>Your refund is being processed. Please allow 3-5 business days for the refund to appear in your account.</p>
      <% } %>
    </div>

    <div class="order-details">
      <h3>Order Summary</h3>
      <p><strong>Order #:</strong> <%= order.id %></p>
      <p><strong>Order Date:</strong> <%= new Date(order.createdAt).toLocaleDateString() %></p>
      <p><strong>Total Amount:</strong> $<%= (order.total_amount || 0).toFixed(2) %></p>
      
      <h4>Cancelled Items</h4>
      <% order.items.forEach(item => { %>
        <div class="order-item">
          <div class="item-image">
            <% if (item.product.image) { %>
              <img src="<%= item.product.image %>" alt="<%= item.product.name %>" style="max-width: 100%; max-height: 100%;">
            <% } else { %>
              <span>No Image</span>
            <% } %>
          </div>
          <div class="item-details">
            <div class="item-name"><%= item.product.name %></div>
            <div>Quantity: <%= item.quantity %></div>
            <div class="item-price">$<%= (item.price * item.quantity).toFixed(2) %></div>
          </div>
        </div>
      <% }); %>
    </div>

    <div style="text-align: center; margin: 30px 0;">
      <a href="<%= process.env.FRONTEND_URL || '#' %>" class="btn">Continue Shopping</a>
      <% if (supportEmail) { %>
        <a href="mailto:<%= supportEmail %>" class="btn secondary">Contact Support</a>
      <% } %>
    </div>

    <div style="background: #f5f5f5; padding: 15px; border-radius: 5px; margin: 20px 0;">
      <h3>Need Help?</h3>
      <p>If you have any questions about your cancellation, please don't hesitate to contact our support team.</p>
      <p>Email: <%= supportEmail || 'support@stylay.com' %></p>
      <p>Phone: <%= process.env.SUPPORT_PHONE || '1-800-123-4567' %></p>
    </div>

    <div class="footer">
      <p>We're sorry for any inconvenience this may have caused.</p>
      <p>© <%= new Date().getFullYear() %> Stylay. All rights reserved.</p>
    </div>
  </div>
</body>
</html>
